version: 0.0
os: linux 

files:
    - source: / 
      destination: /home/ubuntu/back/node-express-realworld-example-app
      timeout: 300
     
      


hooks:
   
   ApplicationStart:
         - location: /script/start_server.sh
           timeout: 300
           runas: root
